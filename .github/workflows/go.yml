name: ci

on:
  push:
    branches: ["*"]
    tags: ["*"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: ">=1.25"
      - run: go vet ./...
      - run: go test ./...
